#include <iostream>
#include <vector>
using namespace std;
int main(){
    int n; cin>>n;
    vector<int> v(n);
    for(int i=0;i<n;i++) cin>>v[i];
    vector<int> res;
    for(int i=0;i<n;i++){
        bool existe=false;
        for(int j=0;j<res.size();j++)
            if(res[j]==v[i]) existe=true;
        if(!existe) res.push_back(v[i]);
    }
    for(int x:res) cout<<x<<" ";
    return 0;
}
