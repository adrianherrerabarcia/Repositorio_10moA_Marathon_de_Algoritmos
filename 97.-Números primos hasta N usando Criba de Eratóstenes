#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<bool> primo(n+1,true);
    primo[0]=primo[1]=false;
    for(int i=2;i*i<=n;i++){
        if(primo[i]){
            for(int j=i*i;j<=n;j+=i) primo[j]=false;
        }
    }
    for(int i=2;i<=n;i++) if(primo[i]) cout<<i<<" ";
}
