#include <iostream>
using namespace std;

int caminos(int n, int m) {
    if (n == 1 || m == 1) return 1;
    return caminos(n - 1, m) + caminos(n, m - 1);
}

int main() {
    int n, m;
    cin >> n >> m;
    cout << caminos(n, m);
}

