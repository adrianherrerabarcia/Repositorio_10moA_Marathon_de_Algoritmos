#include <iostream>
#include <string>
using namespace std;

void invertir(string s){
    if(s.empty()) return;
    invertir(s.substr(1));
    cout<<s[0];
}

int main(){
    string cad;
    cout<<"Cadena: ";
    cin>>cad;
    invertir(cad);
}
