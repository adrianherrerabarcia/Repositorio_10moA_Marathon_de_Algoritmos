#include <iostream>
using namespace std;
int main(){
    int n,suma=0;
    cout<<"Numero: "; cin>>n;
    for(int i=1;i<n;i++) if(n%i==0) suma+=i;
    if(suma==n) cout<<"Perfecto";
    else cout<<"No perfecto";
    return 0;
}
