#include <iostream>
#include <string>
using namespace std;

int main() {
    string texto;
    cout << "Ingrese texto: ";
    getline(cin, texto);
    int cont = 0;
    for (char c : texto)
        if (isupper(c)) cont++;
    cout << "Mayusculas: " << cont;
}
